@page
@model LanguageLandAcademy.Web.Areas.Administration.Pages.Managment.Register.CreateModel
@{
}

@foreach (var t in Model.Registers)
{
    <span>ID</span>
    <span>@t.Id</span>
    <span>PersonId</span>
    <span>@t.PeopleId</span>
    <span>Term</span>
    <span>@t.TermClassId</span>

}

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="card">
                <div class="card-header">
                    افراد ثبت نامی در این ترم / دوره
                </div>
                <div class="card-body">
                    <div class="panel-body">
                        <div>
                        </div>
                        <ul class="list-group overflow-auto" style="height:150px" id="RegisteredPeople">

                      
                        </ul>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="card">
                <div class="card-header">
                    جستجوی کاربران
                </div>
                <div class="card-body">
                    <div class="panel-body">
                        <form class="form-inline row" role="form" method="get">
                            <div class="form-group col-3">
                                <input class="form-control m-2" asp-for="searchModel.Fullname" placeholder="نام">
                            </div>
                          
                            <button type="submit" class="btn btn-success waves-effect waves-light col-1 m-2">جستجو</button>
                            <a class="btn btn-primary waves-effect waves-light col-1 m-2" asp-page="./Index">مشاهده همه</a>
                        </form>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

<div class="row mt-4" id="ProductCategoriesDiv">
    <div class="col-md-12">
        <div class="panel panel-default">

            <div class="card">
                <div class="card-header">
                    لیست اشخاص (@Model.accounts.Count) 
                </div>
                <div class="card-body p-0">
                    <table id="datatable" class="table table-striped table-bordered m-0">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>عکس</th>
                                <th>نام</th>
                                <th>نام پدر</th>
                                <th>نقش</th>
                                <th>عملیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.accounts)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td>
                                        @if (item.ProfilePhoto == "unImage.png" || item.ProfilePhoto == null)
                                        {
                                            <img src="~/images/unImage.png" class="profilePhoto" />
                                        }
                                        else
                                        {
                                            <img src="~/ProfilePictures/@item.ProfilePhoto" class="profilePhoto" />
                                        }

                                    </td>
                                    <td>@item.FullName</td>
                                    <td>@item.FName</td>
                                    <td>@item.Role</td>
                                    <td>
                                        @if (Model.Registers == null)
                                        {
                                            <a class="btn btn-success" onclick="AddPeople(@item.Id,'@item.FullName')">اضافه کردن</a>
                                           @*  <form method="post">
                                                <input type="hidden" asp-for="Registeration.TermClassId" value="@Model.tId" />
                                                <input type="hidden" asp-for="Registeration.PeopleId" value="@item.Id" />
                                                <button type="submit" class="btn tableBtn"
                                                        asp-page-handler="AddToTerm"
                                                        data-toggle="tooltip" data-placement="top" title="بازیابی">
                                                    <i class="icon-plus-circled"></i>
                                                </button>
                                            </form> *@
                                        }
                                        else
                                        {
                                            var regItem = Model.Registers.SingleOrDefault(s => s.PeopleId == @item.Id);
                                            @if (regItem != null)
                                            {
                                                <a class="btn btn-success" onclick="AddPeople(@item.Id,'@item.FullName')">اضافه کردن</a>
                                             @*    <form method="post">
                                                    <button type="submit" class="btn tableBtn"
                                                            asp-page-handler="DeleteFromTerm"
                                                            asp-route-id="@regItem.Id,@regItem.TermClassId"
                                                            asp-page="./Create"
                                                            data-toggle="tooltip" data-placement="top" title="حذف">
                                                        <i class="icon-trash-empty"></i>
                                                    </button>
                                                </form> *@

                                             @*    <form method="post">
                                                    <input type="hidden" asp-for="Registeration.TermClassId" value="@Model.tId" />
                                                    <input type="hidden" asp-for="Registeration.PeopleId" value="@item.Id" />
                                                    <button type="submit" class="btn tableBtn"
                                                            asp-page-handler="DeleteFromTerm"
                                                            data-toggle="tooltip" data-placement="top" title="بازیابی">
                                                        <i class="icon-plus-circled"></i>
                                                    </button>
                                                </form> *@
                                            }
                                            else
                                            {
                                                <a class="btn btn-success" onclick="AddPeople(@item.Id,'@item.FullName')">اضافه کردن</a>
                                            @*     <form method="post">
                                                    <input type="hidden" asp-for="Registeration.TermClassId" value="@Model.tId" />
                                                    <input type="hidden" asp-for="Registeration.PeopleId" value="@item.Id" />
                                                    <button type="submit" class="btn tableBtn"
                                                            asp-page-handler="AddToTerm"
                                                            data-toggle="tooltip" data-placement="top" title="بازیابی">
                                                        <i class="icon-plus-circled"></i>
                                                    </button>
                                                </form> *@
                                            }
                                        }
                                    </td>
                                </tr>

                            }
                        </tbody>
                    </table>
                </div>
            </div>

        </div>
    </div>
</div>